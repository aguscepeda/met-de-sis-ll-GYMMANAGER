services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: gymmanager
      POSTGRES_PASSWORD: gymmanager
      POSTGRES_DB: gymmanager
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: